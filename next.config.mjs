/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['randomuser.me'],
    // If you have other domains already, add them too:
    // domains: ['example.com', 'randomuser.me', ...],
  },
  // ... your other existing config
};

export default nextConfig;